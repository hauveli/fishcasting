package hauveli.fishcasting.networking.handler
/*
import hauveli.fishcasting.networking.msg.*
import io.wispforest.owo.network.ServerAccess

fun FishcastingMessageC2S.applyOnServer(access: ServerAccess) = access.player().server.execute {
    // NOTE: this is commented out because otherwise it fails to compile if there's nothing inside of the when expression
    /*
    when (this) {
        is MsgExampleNameC2S -> {
           handleMessage(...)
        }
        // add server-side message handlers here
    }
    */
}
*/